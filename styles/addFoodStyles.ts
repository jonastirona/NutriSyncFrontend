import { StyleSheet } from 'react-native';

const addFoodStyles = StyleSheet.create({
    button: {
        backgroundColor: '#d11ffa', // Bright Pink
        paddingVertical: 10,
        paddingHorizontal: 20,
        borderRadius: 5,
        alignItems: 'center',
        marginVertical: 10,
    },
    buttonText: {
        color: '#18183C', // Deep Blue
        fontWeight: 'bold',
    },
});

export default addFoodStyles;